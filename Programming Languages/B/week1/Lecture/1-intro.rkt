;;; Always include this in the first line to tell the program that
;;; this file uses racket language.
#lang racket

;;; A workaround so we can write tests 
(provide (all-defined-out))

(define s "hello")